# Readerly v1 — Archive

These are the original **Readerly v1** static site files, kept here for reference only:

- `index.html`
- `script.js`
- `style.css`

They are **not** part of the v2 build. The v2 frontend lives in `../readerly-frontend`
and the API in `../readerly-api`. Nothing in this folder is deployed or maintained —
it's preserved as a snapshot of the original single-page site.
